// Deprecated compatibility wrapper.
// New admin callers should prefer '@/modules/channels/api'; this file remains
// only as a re-export until every legacy caller is migrated.
export * from '@/modules/channels/api/public'
